Create a C++ actor class
Explaing header and cpp files.
Create BP of the actor in editor. Add static mesh and create an instance to the level.
add UE_LOG to BeginPlay
Show displaying location in UE_LOG. (using FString)
  FString Loc = GetActorLocation().ToString();
Try on Tick

Show SetActorLocation()
Try SetActorLocation() on Tick so that actor drifts to the air.

Show the C++ actor added as Blueprint actor.
Show UProperty. (EditAnywhere, VisibleAnywhere, EditDefaultsOny (with category))

Using the above show how to add explosion effect to projectile.

C++
-----------
Projectile header file
--------------------------------------------
UPROPERTY(EditDefaultsOnly, Category = "FX")
UParticleSystem* ExplosionEffect;
Forward declaration required.
class UParticleSystem;


Projectile cpp file
---------------------------------------------
Add headerfile GameplayStatics.h
UGameplayStatics::SpawnEmitterAtLocation(GetWorld(), ExplosionEffect, GetActorLocation());
